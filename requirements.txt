# Core ML
torch>=2.0
torchvision>=0.15

# TensorRT (requires NVIDIA TensorRT 10.x installed system-wide)
# Install via: pip install torch-tensorrt
# Or: pip install tensorrt
torch-tensorrt
tensorrt

# Computer Vision
opencv-python>=4.8

# OSC (for VMT / VR tracking output)
python-osc>=1.8

# GUI
PySide6>=6.5
qasync>=0.27

# Microsoft Store integration (Windows only)
# pip install winrt-Windows.Services.Store winrt-runtime
# These are Windows-only packages and will fail on Linux/macOS

# NumPy
numpy>=1.24
